# Moire

Образец сайта интернет-магазина, реализованный на Vue.js(2).
На сайте работает каталог с пагинацией и фильтрацией товаров, управление корзиной, 
формирование заказа (с расчётом итоговой суммы), валидация формы. 

Работа с Товарами и заказами использует REST API и  менеджер состояний вью (vuex).

## Сборка и запуск проекта
1) Клонируйте проект из ветки development
2) Установите пакеты при помощи привычного мендежера пакетов:
``` pnpm i ``` (пример для pnpm)
3) соберите дев-версию:
``` pnpm dev ``` (пример для pnpm)
или продакшн-версию:
``` pnpm build ``` (пример для pnpm)
4) для дев-версии:
   перейдите по ссылке на localHost из консоли.
Для билд-версии:
запустите index.html из папки dist.

Посмотреть можно здесь:
https://sm1lent.github.io/Moire/
